name: fpi-e-register deployment

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up PHP with Composer
      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.2" # Adjust PHP version to match your project
          tools: composer

      # Step 3: Install dependencies using Composer
      - name: Install dependencies
        run: composer install --no-dev --optimize-autoloader

      # - name: Run database migrations
      #   run: php artisan migrate --force
      #   env:
      #     DB_CONNECTION: mysql
      #     DB_HOST: 127.0.0.1
      #     DB_PORT: 3306
      #     DB_DATABASE: federalpolyilaro_fpi_e-register_db
      #     DB_USERNAME: federalpolyilaro_sug_election_user
      #     DB_PASSWORD: fp!}rNB,kcTH

      # Step 4: Upload files to cPanel via FTP
      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.2.0
        with:
          server: 192.64.84.226
          username: federalpolyilaro
          password: N3w5ever@ilar
          server-dir: /public_html/fpieregistry.federalpolyilaro.edu.ng/public/

      - name: Run Migrations via SSH
        uses: appleboy/ssh-action@master
        with:
          host: 192.64.84.226
          username: federalpolyilaro
          password: N3w5ever@ilar
          script: |
            cd /public_html/fpieregistry.federalpolyilaro.edu.ng/
            composer install
            php artisan migrate --force
